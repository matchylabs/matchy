[package]
name = "mdbook-project-version"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mdbook-project-version"
path = "src/main.rs"

[dependencies]
mdbook-preprocessor = "0.5"
anyhow = "1.0"
serde_json = "1.0"
toml = "0.8"
